# Feature 06 — Suporte a Gradle — Tasks

**Fundação (leitura) implementada** na Fase 15 (`specs/phases/phase-15-gradle-fundacao/`): `GradleAdapter.detect`/`infer_structure`, Groovy e Kotlin DSL, árvore de módulos + group/version + dependências (diretas e gerenciadas via `java-platform`/`constraints{}` ou `platform(...)` externo) extraídos por um parser baseado em regex + contagem de chaves (subconjunto convencional da linguagem). Registrado em `services/adapters_registry.py`; nenhuma mudança na TUI foi necessária.

**Diretórios implementados** na Fase 16 (`specs/phases/phase-16-gradle-diretorios/`): `GradleAdapter.add_directory`/`remove_directory`, primeira mutação real — possível sem editar `build.gradle(.kts)` porque diretórios-padrão não precisam de declaração explícita no Gradle (igual ao Maven). `find_module`/`resolve_module_relative_path` extraídos para `manager.adapters.common.lookup`, compartilhados com o `MavenAdapter`.

Ainda fora de escopo: as demais 9 mutações (`add_module`, `remove_module`, `update_dependency`, `update_metadata`, `remove_dependency`, `remove_direct_dependency`, `create_project`, `register_directory_role`, `unregister_directory_role` — stubs `NotImplementedError`, todas exigem editar `build.gradle(.kts)` como texto); version catalogs; `allprojects{}`/`subprojects{}`; `sourceSets{}` customizado; árvore de módulos verdadeiramente aninhada (hoje achatada sob a raiz).
